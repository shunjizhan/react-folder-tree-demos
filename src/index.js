import React from 'react';
import { render } from 'react-dom';
import FolderTreeDemos from './components/FolderTreeDemos';

const root = document.getElementById('root');
render(<FolderTreeDemos />, root);
